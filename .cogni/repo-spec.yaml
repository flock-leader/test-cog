# Generated by Node Formation — node identity + governance.
# node_id is the single identity authority (REPO_SPEC_IS_IDENTITY_SSOT);
# scope_id is derived as uuidv5("default", node_id) and must never change.
# See docs/spec/identity-model.md, docs/spec/node-formation.md.

schema_version: "0.1.4"

node_id: "cea5ddea-a18d-44cd-8ffb-4fdd1fd104ac"
scope_id: "17be4e5a-48a4-5f61-912e-8cf5b32e5bd3"
scope_key: "default"

cogni_dao:
  dao_contract: "0xc7e7263717fbfe25a3b8c422315e33dccf9c1db9"
  plugin_contract: "0x44f0fb4c06b2b3168ab2ebb53af85669583fc682"
  signal_contract: "0x3449010de932887ee6b239f6e385f423bef469a6"
  chain_id: "8453"

# Operator wallet configuration (Privy-managed)
# address: Privy-managed EOA that receives operator share from Split
# Split contract address is written to payments_in.credits_topup.receiving_address after activation.
operator_wallet:
  address: "0xdCCa8D85603C2CC47dc6974a790dF846f8695056"

knowledge:
  database: "knowledge_test_cog"
  remote:
    provider: dolthub
    owner: "cogni-test-nodes"
    repo: "knowledge-test-cog"
    url: "https://doltremoteapi.dolthub.com/cogni-test-nodes/knowledge-test-cog"
    custody: cogni-owned

# Payment rails — activate with: pnpm node:activate-payments
payments:
  status: pending_activation

# Born-reviewable default gates (BORN_REVIEWABLE). No `nodes:` registry → the operator's
# review path resolves this minted repo as a single-node fork and runs these gates against
# the repo-root `.cogni/rules/` shipped by the node-template. Tune per node post-mint.
gates:
  - type: review-limits
    id: review_limits
    with:
      max_changed_files: 50
      max_total_diff_kb: 1500
  - type: ai-rule
    with:
      rule_file: pr-syntropy-coherence.yaml
  - type: ai-rule
    with:
      rule_file: patterns-and-docs.yaml
  - type: ai-rule
    with:
      rule_file: repo-goal-alignment.yaml
